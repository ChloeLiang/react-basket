import React, { FC } from 'react';
import MenuContainer from '../containers/MenuContainer';

const App: FC = () => {
  return (
    <div>
      <MenuContainer />
    </div>
  );
};

export default App;
